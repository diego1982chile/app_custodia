package test3.ncxchile.cl.greenDAO;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;
import test3.ncxchile.cl.db.Global;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table CLIENTE.
*/
public class ClienteDao extends AbstractDao<Cliente, Long> {

    public static final String TABLENAME = "CLIENTE";

    /**
     * Properties of entity Cliente.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Licencia = new Property(1, String.class, "Licencia", false, "LICENCIA");
        public final static Property PersonaID = new Property(2, long.class, "personaID", false, "PERSONA_ID");
        public final static Property ClientePropietarioID = new Property(3, long.class, "ClientePropietarioID", false, "CLIENTE_PROPIETARIO_ID");
    };

    private DaoSession daoSession;

    private Query<Cliente> vehiculoData_ClientePropietarioQuery;

    public ClienteDao(DaoConfig config) {
        super(config);
    }
    
    public ClienteDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'CLIENTE' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'LICENCIA' TEXT," + // 1: Licencia
                "'PERSONA_ID' INTEGER NOT NULL ," + // 2: personaID
                "'CLIENTE_PROPIETARIO_ID' INTEGER NOT NULL );"); // 3: ClientePropietarioID
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'CLIENTE'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Cliente entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String Licencia = entity.getLicencia();
        if (Licencia != null) {
            stmt.bindString(2, Licencia);
        }
        stmt.bindLong(3, entity.getPersonaID());
        stmt.bindLong(4, entity.getClientePropietarioID());
    }

    @Override
    protected void attachEntity(Cliente entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Cliente readEntity(Cursor cursor, int offset) {
        Cliente entity = new Cliente( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // Licencia
            cursor.getLong(offset + 2), // personaID
            cursor.getLong(offset + 3) // ClientePropietarioID
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Cliente entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setLicencia(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setPersonaID(cursor.getLong(offset + 2));
        entity.setClientePropietarioID(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Cliente entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Cliente entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "ClientePropietario" to-many relationship of VehiculoData. */
    public List<Cliente> _queryVehiculoData_ClientePropietario(long ClientePropietarioID) {
        synchronized (this) {
            if (vehiculoData_ClientePropietarioQuery == null) {
                QueryBuilder<Cliente> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ClientePropietarioID.eq(null));
                queryBuilder.orderRaw("CLIENTE_PROPIETARIO_ID ASC");
                vehiculoData_ClientePropietarioQuery = queryBuilder.build();
            }
        }
        Query<Cliente> query = vehiculoData_ClientePropietarioQuery.forCurrentThread();
        query.setParameter(0, ClientePropietarioID);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPersonaDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getVehiculoDataDao().getAllColumns());
            builder.append(" FROM CLIENTE T");
            builder.append(" LEFT JOIN PERSONA T0 ON T.'PERSONA_ID'=T0.'_id'");
            builder.append(" LEFT JOIN VEHICULO_DATA T1 ON T.'CLIENTE_PROPIETARIO_ID'=T1.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Cliente loadCurrentDeep(Cursor cursor, boolean lock) {
        Cliente entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Persona persona = loadCurrentOther(daoSession.getPersonaDao(), cursor, offset);
         if(persona != null) {
            entity.setPersona(persona);
        }
        offset += daoSession.getPersonaDao().getAllColumns().length;

        VehiculoData vehiculoData = loadCurrentOther(daoSession.getVehiculoDataDao(), cursor, offset);
         if(vehiculoData != null) {
            entity.setVehiculoData(vehiculoData);
        }

        return entity;    
    }

    public Cliente loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Cliente> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Cliente> list = new ArrayList<Cliente>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Cliente> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Cliente> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }

    public Cliente getByRut(String rut){
        Persona persona=Global.daoSession.getPersonaDao().getByRut(rut);

        if(persona!=null){
            List clientes= queryBuilder()
                    .where(Properties.PersonaID.eq(persona.getId()))
                    .list();
            if(clientes.size()>0)
                return (Cliente)clientes.get(clientes.size()-1);
        }
        return null;
    }
 
}
