<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#30000000"
                android:centerColor="#20000000"
                android:endColor="#30000000"
                android:angle="270"
                />
            <corners
                android:radius="6dp" />
            <stroke
                android:width="1dp"
                android:color="#70000000" />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#f60f7900"
                    android:centerColor="#6126b700"
                    android:endColor="#f60f7900"
                    android:angle="270"
                    />
                <corners
                    android:radius="6dp" />
                <stroke
                    android:width="1dp"
                    android:color="#70000000" />
            </shape>
        </clip>
    </item>
</layer-list>